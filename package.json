{
  "name": "cardmaster",
  "version": "3.0.0",
  "engines": {
    "node": ">=16.9.0"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -r build/*",
    "test": "rm -r build/* ; tsc && node ./build/app.js",
    "nuke": "rm -r build/* ; tsc && node ./build/nukecommands.js",
    "deploy": "rm -r build/* ; tsc && node ./build/deploycommands.js",
    "build": "rm -r build/* ; tsc",
    "start": "node ./build/app.js"
  },
  "author": "Astavie",
  "license": "ISC",
  "dependencies": {
    "@discordjs/builders": "^0.13.0",
    "@discordjs/rest": "^0.4.1",
    "@types/node": "^17.0.25",
    "discord-api-types": "^0.31.2",
    "discord.js": "^13.6.0",
    "dotenv": "^8.2.0",
    "typescript": "^4.6.3",
    "word-list-json": "^0.2.0"
  }
}
